# Project improvement backlog

Suggestions surfaced while doing real work in this project — one entry per
task, per `prompts/project-improvement.md`'s "Log it" step. Committed and
shared, same as `logs/history.jsonl`: the point of this file is that a
rough edge one teammate's session hits becomes visible to everyone, not
just rediscovered independently session after session. The two files split
by content, not by visibility — this one is a process-improvement backlog
about the project itself, `logs/history.jsonl` is an event log of what was
built for a client account.

This is a backlog, not a changelog — an entry landing here is not itself
an action taken. Turning one into a real change to `prompts/`,
`references/`, `scripts/`, or `CLAUDE.md` is a separate, explicitly
confirmed step, exactly like any other change to this project's own files.

Append new entries under **Open**, oldest first. When an entry is acted on
(or deliberately declined), move it to **Resolved** with a one-line note
on what changed (or why it was declined) and the date.

## Open

<!-- Append new suggestions below this line, one entry per task:

- 2026-08-19 — account 662 — workflow-friction — Requirements Intake, recruiter performance dashboard
  The currency question gets asked even when every chart in the request is
  a plain count — worth skipping it until a monetary field is actually
  about to be formatted.
-->

- 2026-09-18 — general — core-function-quality — Retrofit the two automated dashboard scripts against the rebuilt visual-design-standards.md
  `references/visual-design-standards.md` was rebuilt into a full, research-grounded design system (dashboard information hierarchy, dual-axis ban, ordinal color for pipeline stages, cross-dashboard entity-color consistency, density-aware data labels, KPI trend-direction color) and CLAUDE.md's "Color"/"Data labels"/"KPI trend color direction" sections now point to it and explicitly flag both automated scripts as not yet compliant. Neither `scripts/default_dashboard_template.json` nor `scripts/important_metrics_dashboard_template.json` sets any `series_settings` color, distinguishes label density by chart cardinality, or checks its `smartscalar` cards' comparison-direction setting (e.g. "Total Cost of Calls" trending up is presented with Metabase's raw increase-is-green default, unexamined). This is a deliberate, separate follow-up — the user explicitly didn't want the current templates treated as the reference example while the standard itself was being built.
  → Partially addressed 2026-09-24: the `series_settings`/pie-color third is now done (see the 2026-09-24 "consistency" entry below, moved to Resolved) — every pie and multi-series bar/line/row/area card in both templates now gets an explicit color at creation time. Label-density-by-cardinality and the `smartscalar` comparison-direction check are still outstanding for both scripts — this entry stays Open for those two.

- 2026-09-24 — account 662 — consistency — Verifying the new dynamic pie-color logic against account 662's real data (scripts/default_dashboard_template.json, "Distrbution of Calls by Call Type")
  This card's `graph.dimensions` is `["call_type", "created_on"]` — call_type first, the month breakout second. Per Metabase's own documented convention ("a second dimension in `graph.dimensions` becomes a series breakout" — the `visualization` skill), this means the card's actual series breakout is by *month*, not by call type, despite the card's name — the sibling card "Calls made by users over time" instead orders its two dimensions date-first, name-second (date on the x-axis, the non-temporal dimension as the series), which is the more common/expected pattern for a two-dimension time chart. Not changed here since flipping the dimension order changes which axis the chart actually renders on (a real behavior change, not a coloring fix) and it's unclear whether the current order is a genuine authoring slip or an intentional "compare call-type mix month over month" design — worth a look against the original source dashboard (12908) to confirm intent before touching it.

## Resolved

- 2026-09-24 — general — consistency — Project improvement review (grounded in docs/workflow.md's "In every flow" section vs. docs/architecture.md and today's earlier .claude/settings.json fix)
  Today's earlier fix (see the ".claude/settings.json" Resolved entry below) demoted the history-log-enforcement hook to a local, git-ignored, per-developer convenience and rewrote "docs/architecture.md's two mentions of the hook" to say so — but `docs/workflow.md`'s own closing "In every flow" section still stated, unhedged, "for the Requirements Intake flow this is enforced by a Claude Code hook that flags the session if a card was created or a dashboard was created/updated but never logged," with no mention that it's optional/local/git-ignored. `docs/workflow.md` bills itself as "the same flows... written for a teammate reading it," so a teammate relying on this file would have believed logging is always enforced when it isn't on a fresh clone.
  → Resolved 2026-09-24: rewrote `docs/workflow.md`'s "In every flow" section to match `docs/architecture.md`'s corrected framing — the hook is now described as something a teammate can optionally set up locally, git-ignored, not distributed or enforced centrally, so its absence on a fresh clone isn't a surprise.

- 2026-09-24 — general — consistency — Project improvement review (grounded in scripts/default_dashboard_template.json and scripts/important_metrics_dashboard_template.json vs. CLAUDE.md's "Color" section and the entry above)
  CLAUDE.md's "Color" section and the entry above both asserted neither dashboard script's template sets any `series_settings` color on any card — checked directly and that was no longer accurate: `scripts/default_dashboard_template.json` had 9 of its 15 graphable (pie/bar/line/row) cards with `series_settings` already set, using the documented categorical palette hex values, and `scripts/important_metrics_dashboard_template.json` had 3 of its 6. The real remaining gap was narrower: all 6 pie-display cards across both templates, plus 3 multi-series bar/line cards in the default template, carried zero color assignment.
  → Resolved 2026-09-24: rather than hardcode colors for that specific card list (account-specific category values — e.g. Call Type, Deal Stage, City — can't be known at template-authoring time and differ per account), added `apply_series_colors`/`assign_categorical_colors`/`rank_values_by_metric` to both scripts: at card-creation time, a pie's slices or a multi-series card's series get colored by running that card's own already-validated query live and assigning the fixed 8-hue categorical palette to whichever real category values actually dominate the chart (by summed metric total, never by value-ranking the color's intensity). Verified end-to-end against real account 662 data before shipping (read-only `mb query` runs, no cards created) — this caught two real bugs the static-hardcoding approach would have hidden: (1) the discovery query hit `mb`'s default 24KB output cap on a high-cardinality field ("City," 4177 distinct values) and had to be re-run with `--max-bytes 0`; (2) "Candidate Distribution By Work Experience" already carries a hand-built, correctly ordinal `pie.rows` color ramp (light→dark blue by experience band) that the first version of the guard didn't recognize (it only checked for `pie.colors`) and nearly overwrote with nominal colors — fixed by also checking for `pie.rows`/`series_settings` before touching a card. CLAUDE.md's "Color" section and both scripts' module docstrings now describe this accurately. Testing also surfaced two separate, out-of-scope findings, logged below.

- 2026-09-24 — general — consistency — Project improvement review (grounded in README.md's "Project structure" and docs/architecture.md's "Files" list vs. prompts/documentation.md and CLAUDE.md's "Dashboard documentation" section)
  `prompts/documentation.md` (the Dashboard Documentation method — TipTap body shape, grounding every chart paragraph in its real `dataset_query`, per-chart filter/drill-down applicability, the card-cloning gotcha log) is already wired into CLAUDE.md's "Dashboard documentation" section and referenced 6 times in `prompts/requirements-intake.md`, but was missing entirely from both README.md's "Project structure" file tree and `docs/architecture.md`'s "Files" enumeration — both list every other `prompts/*.md` file (including the legacy, superseded `metabase_skill_improvement.md`) but omitted this one.
  → Resolved 2026-09-24: added `prompts/documentation.md` to both file trees with the same one-line treatment every other `prompts/*.md` entry gets.

- 2026-09-24 — general — metabase-currency — Project improvement review (mandatory `mb skills list` diff against CLAUDE.md's skill paragraph)
  `mb skills list --json` now returns 11 bundled skills, not the 10 CLAUDE.md's "Before every session" paragraph accounts for. The new one is `transform` ("Author and run Metabase transforms via `mb` — body shape, create + run-with-wait, run inspection, dependencies, cancel..."). CLAUDE.md has no mention of it anywhere — unlike `git-sync` and `data-workflow`, the two other bundled-but-unused skills, both of which get an explicit "bundled, not used, here's why" paragraph. `mb auth list --json`'s live `tokenFeatures` for both profiles confirm `transforms-basic: false` and `transforms-python: false` — unchanged from CLAUDE.md's existing 2026-09-17 snapshot, so the plan genuinely doesn't support it today — but nothing currently tells a future session that this skill exists and is plan-gated off, the way the `tokenFeatures` paragraph already does for `metabot-v3`. A session that runs `mb skills list` directly (rather than only reading CLAUDE.md) could spend real effort attempting a transform before discovering the plan blocks it.
  → Resolved 2026-09-24: added a paragraph to CLAUDE.md's "Before every session" section, right after the `git-sync`/`data-workflow` paragraphs, giving `transform` the same explicit "bundled but not used, here's why" treatment — naming it, confirming `transforms-basic`/`transforms-python` are both `false` on this instance, and noting to re-check `tokenFeatures` rather than assume that stays true.

- 2026-09-24 — account 662 — consistency — Project improvement review (mandatory memory-layer consistency check, grounded in logs/history.jsonl's `default_dashboard_created` entries vs. references/metric-glossary.md)
  `scripts/create_default_dashboard.py` confirms a per-account currency (via `--currency` or its own interactive prompt) exactly like CLAUDE.md's "ask once per account" convention describes, but — unlike every conversational flow (`prompts/discovery.md`, `prompts/chart-generation.md`) — never wrote that confirmed value back to `references/metric-glossary.md`, only to `logs/history.jsonl`'s own `default_dashboard_created` entry (an audit-trail fact, not the shared per-account memory a later Requirements Intake session actually checks before re-asking). Confirmed concretely: account 662 has two `default_dashboard_created` entries (2026-09-15, `currency: "USD"` both times) but, until this fix, no `## Account 662` section in `metric-glossary.md` at all — the exact same failure mode as the already-reviewed "5 accounts with real chart work, zero glossary sections" gap (2026-09-17 entry below), just reached through the Default Dashboard script's own currency prompt instead of a conversational chart build, so that earlier fix (scoped only to conversational write-back points) never covered it.
  → Resolved 2026-09-24: added `record_currency_in_glossary()` to `scripts/create_default_dashboard.py`, called right after a successful `default_dashboard_created` log — it creates a `## Account <n>` section if none exists, adds a `**Currency:**` line to an existing section that lacks one, and (tested directly) leaves an existing, differently-valued Currency line untouched with a printed warning rather than silently overwriting it; it also correctly skips the literal `## Account 662` example inside this file's own "## Account sections" fenced-code walkthrough rather than mistaking it for a real entry (verified live against three cases: new section, idempotent same-value re-run, and a deliberately conflicting value). Backfilled account 662's real, already-confirmed `USD` value (from `logs/history.jsonl`, not guessed) into a new `## Account 662` section. Updated CLAUDE.md's "Value formatting" paragraph and the script's own module docstring to document the write-back.

- 2026-09-24 — account 62956 — architecture — Project improvement review (grounded in logs/history.jsonl's `tv_dashboard_created`/`tv_dashboard_updated` entries vs. CLAUDE.md's three named flows)
  `logs/history.jsonl` records a "TV Dashboard" pattern for account 62956 on 2026-08-31 — 5 wall-display dashboards (no-scroll grid sizing, photo-placeholder substitution, a treemap-from-example-card technique) logged under `tv_dashboard_created`/`tv_dashboard_updated` types that appear nowhere in CLAUDE.md's "History log" section, with no mention of a TV/wall-display dashboard case anywhere in CLAUDE.md's three named flows, `prompts/`, or `references/canonical-patterns.md`.
  → Declined 2026-09-24: the user confirmed this isn't a real project concept or flow — it was a one-off historical build, not a pattern worth formalizing into CLAUDE.md, a prompt, or canonical-patterns.md. No change made to any project file. Logged here specifically so a future project-improvement review sees this was already raised and declined, rather than resurfacing the same suggestion off the same `logs/history.jsonl` entries every time it runs.

- 2026-09-24 — general — robustness — Project improvement review (grounded in prompts/performance-tracking.md's own "Pending-entry check" vs. its "Superseding an older entry" section, verified against real log data)
  `prompts/performance-tracking.md`'s "Superseding an older entry" section establishes that "any `tracking_id` appearing in another entry's `supersedes` list is excluded from totals/aggregation — its replacement entry is the authoritative one for that work." But the "Pending-entry check (Step 0 hook)" section's matching logic doesn't inherit that exclusion — it just says to "scan `logs/performance-tracking.jsonl` for `requirement_pending` entries with no `requirement_confirmed` entry sharing their `tracking_id`," with no mention of `supersedes` at all. `logs/performance-tracking.jsonl` already has the exact scenario this breaks on: account 89060's drill-down work went through three same-session corrections (`...200310...`, `...210531...`, `...220137...cleveland-clinic-drilldowns`, each logged as its own `requirement_pending` entry superseding the last, per the file's documented correction pattern) before a final entry (`...221320...`) was confirmed. The three superseded entries never got their own `requirement_confirmed` entry — they were never meant to — so a literal reading of "Pending-entry check" would flag all three as still awaiting the user's confirmation every session from now on, permanently, alongside any genuinely new pending work. Confirmed by directly scanning the log: as of today, all 4 tracking_ids for this batch still show up as "pending" by the check's stated matching rule, even though the work was fully confirmed 6 days ago (2026-09-18) under the final tracking_id.
  → Resolved 2026-09-24: rewrote "Pending-entry check (Step 0 hook)" to first exclude any `tracking_id` appearing in another entry's `supersedes` array — whether that later entry is itself `pending` or `confirmed` — before checking for a matching `requirement_confirmed` entry, explicitly noting a same-session correction can chain through several `requirement_pending` entries (each superseding the last) before the work is ever confirmed, and only the final un-superseded id in that chain needs checking. Verified the corrected logic against the live log: applying the exclusion to all 23 logged batches leaves zero false positives (the three dead account-89060 entries are now correctly excluded, and no other hidden unconfirmed work surfaced).

- 2026-09-24 — general — robustness — Project improvement review (grounded in .gitignore vs. scripts/'s actual Python artifacts)
  `.gitignore` has no `__pycache__/`/`*.pyc` entry at all despite this project having real `.py` scripts (`scripts/create_default_dashboard.py`, `create_important_metrics_dashboard.py`, `dashboard_drilldowns.py`) that generate exactly that clutter on every run — `scripts/__pycache__/` (currently holding `create_important_metrics_dashboard.cpython-312.pyc` and `dashboard_drilldowns.cpython-312.pyc`) has shown up as an untracked (`??`) directory in `git status` in every check this session, confirmed via `git check-ignore -v scripts/__pycache__` returning nothing (exit 1 = not ignored). A future broad `git add -A`/`git add .` would commit compiled bytecode into the repo. Worth adding a standard `__pycache__/`/`*.py[cod]` entry alongside the existing "OS / editor cruft" section.
  → Resolved 2026-09-24: added `__pycache__/` and `*.py[cod]` to `.gitignore` under a new "Python bytecode cache" section. Verified live: `git check-ignore -v scripts/__pycache__` now resolves to the new rule, and the directory no longer appears in `git status`.

- 2026-09-24 — general — robustness — Project improvement review (grounded in .gitignore vs. docs/architecture.md and .claude/settings.json's own git history)
  `.gitignore` lines 14-16 group `.claude/settings.local.json` and `.claude/settings.json` under one "# Local Claude Code settings" comment and ignore both — but `.claude/settings.json` is not actually local/personal: `git ls-files`/`git log -- .claude/settings.json` confirm it's tracked with real commit history (e.g. "2b2a7c0 Revert unintended python3 -c auto-allow permission in .claude/settings.json"), and `docs/architecture.md` explicitly documents it as a shared, standing guardrail — "A `.claude/settings.json` hook enforces the history-log step for the conversational flow... it flags (via a `Stop` hook) if `mb card create` / `mb dashboard create` / `mb dashboard update` ran but `logs/history.jsonl` was never appended to." It currently works only because the file was already tracked before the ignore rule was added (git doesn't retroactively untrack a file just because `.gitignore` later lists it) — but the rule itself is live and wrong: it would silently stop tracking any future accidental `git rm --cached` of the file with no warning, and a teammate reading `.gitignore`'s own comment has no reason to think this file is meant to be shared/committed at all, unlike `settings.local.json` (genuinely untracked, correctly grouped).
  → Resolved 2026-09-24: the user chose to resolve the mismatch by making reality match `.gitignore`, not the other way around — `.claude/settings.json` (including its history-log-enforcement hook) is now deliberately a local, per-developer convenience, not a team-wide guardrail. `git rm --cached .claude/settings.json` untracked it (file kept on disk, unchanged) and staged the removal — not yet committed, left for the user's own review. `docs/architecture.md`'s two mentions of the hook were rewritten to say it's a git-ignored, optional, per-developer setup that this repo doesn't distribute or enforce centrally, rather than describing it as always-on for the team.

- 2026-09-24 — general — consistency — Project improvement review (grounded in references/canonical-patterns.md vs. logs/history.jsonl)
  `references/canonical-patterns.md` still reads "(no patterns recorded yet)" despite `prompts/chart-generation.md` step 8 requiring, in the same turn a card is created, "if this card's underlying construction (joins, ranking/summarize logic) is genuinely reusable beyond this one chart... record it in references/canonical-patterns.md" — the same immediate-write-back discipline already enforced for `references/metric-glossary.md`. `logs/history.jsonl` has a concrete instance the rule was skipped for: account 89060's card 76245 ("Outreach Activity - Cold Calls Variant") is logged with its own note stating it was "promoted to a Model since it backs 2 charts (Weekly Outreach, Outreach Activity pie)" — literally the file's own qualifying bar — yet no canonical-patterns.md entry exists for it, or for any other construction across the 8 accounts with real chart work in the log (e.g. the stage-ordinal current-stage `CASE` ranking CLAUDE.md itself repeatedly cites as the canonical example of reusable logic worth promoting). Unlike the metric-glossary gap (already reviewed and deliberately left unreconstructed for 5 accounts, per the 2026-09-17 Resolved entry below), this file's population has not been reviewed or declined before — it appears to simply not be happening in practice.
  → Resolved 2026-09-24, in two parts: (1) retroactively populated `references/canonical-patterns.md` with the one concrete, already-qualifying case — verified live against both cards' real `dataset_query` (`mb card get 76245`/`52671 --full --json`, not the log note alone or memory) — a new "Outreach/engagement activity classification via UNION across heterogeneous log tables" pattern documenting the shared four-column branch construction, with Models #52671 and #76245 as the two confirmed instances and an explicit "When NOT to reuse it" noting the per-branch filter *values* don't generalize even within the same account, only the shape does. Unlike the metric-glossary case, this carried no "was it actually user-confirmed" risk — a pattern is a read-off of a card's own already-built query, not a business-term assertion — so reconstruction was safe here. (2) Added a matching check to `prompts/requirements-intake.md`'s "Final recap" section (which already re-reads metric-glossary.md before wrapping up) — now also lists every session-built card whose construction was genuinely reusable and re-reads canonical-patterns.md to confirm each got an entry, catching a missed write before the session ends instead of relying on remembering step 8's clause.

- 2026-09-24 — general — consistency — Project improvement review (grounded in CLAUDE.md's new Segment/Measure paragraph vs. its own "Avoiding duplicate charts" section)
  CLAUDE.md's "Chart creation" section now tells a session to save a reusable single-table filter/aggregation as a Segment (`mb segment create`) or Measure (`mb measure create`), but nothing there — or in "Avoiding duplicate charts," which explicitly scopes its `mb search <term> --models card,dashboard` dedup check to cards and dashboards only — tells a session to check for an existing same-purpose Segment/Measure first. `mb search --models` genuinely supports `segment`/`measure` as search model types (confirmed via `mb search --help --json`'s output schema), so the check is cheap and consistent with the pattern already used for cards/dashboards; without it, two sessions on the same account could silently create two differently-named Segments encoding the same business filter (e.g. "active client").
  → Resolved 2026-09-24: added a dedup step to the front of CLAUDE.md's Segment/Measure paragraph — `mb search <term> --models segment,measure --limit 20 --json` before creating either, reusing an existing match on the same table instead of creating a near-duplicate under a different name, matching "Avoiding duplicate charts"'s own principle.

- 2026-09-24 — general — consistency — Project improvement review (grounded in docs/workflow.md vs. CLAUDE.md/prompts/requirements-intake.md's new documentation-only case)
  `docs/workflow.md`'s "Requirements Intake flow" (steps 1-11) still only narrates the chart-building path end to end — it doesn't mention the "documentation-only request" case (an existing dashboard, no new chart work, just a companion Document) that was just added to CLAUDE.md's "Dashboard documentation" section and `prompts/requirements-intake.md`'s "Add the documentation Document" section. `docs/workflow.md`'s own header states its purpose is "the same flows described in CLAUDE.md, written for a teammate reading it rather than for Claude executing it" — so a teammate reading it now sees a narrower Requirements Intake than what the flow actually supports.
  → Resolved 2026-09-24: added a note at the top of `docs/workflow.md`'s "Requirements Intake flow" section describing the documentation-only case in the same plain-English style as the rest of the file — it skips the chart-building steps and the Data Team WIP-vs-account-collection question, going straight to the existing documentation step for the named dashboard.

- 2026-09-23 — general — metabase-currency — Project improvement review (mandatory `mb skills list`/`mb --help --json` diff against CLAUDE.md's skill paragraph)
  `mb --help --json` exposes `segment create`/`update` (a saved, reusable filter) and `measure create`/`update` (a saved, reusable aggregation), and `card create`'s own description is literally "Create a card (question, model, or metric) from JSON" — a native reusable Metric card type distinct from a plain question — yet CLAUDE.md never mentions any of the three, and no file under `prompts/`, `references/`, or `scripts/` does either (`grep` confirms zero hits outside unrelated English uses of the words "segment"/"measure"). This isn't hypothetical: `logs/performance-tracking.jsonl`'s 2026-09-18 entry for account 92840 root-causes a `built_modified` card on exactly this shape — "the client-status filter reflects a segment definition ('active client' = status in Active/Renewing) that was never asked about up front" — i.e. a business concept that is literally a Metabase Segment, currently only ever encoded ad hoc inside each card's own filter/expression steps per "Query transparency" rather than defined once and reused. Separately, `mb skills list --json` also lists a `data-workflow` skill ("guided end-to-end: raw data → clean tables → metrics → answers → dashboards") that CLAUDE.md's "Before every session" paragraph doesn't mention alongside `mbql`/`dashboard`/`visualization`/`document`/`metadata`/`native-sql`/`notification` — worth a deliberate look at whether `segment`/`measure`/the `metric` card type belong in this project's "Chart creation" escalation path (alongside or instead of promoting reusable logic to a Model), and whether `data-workflow` is relevant to this project's own discovery/build loop or a deliberate non-fit worth noting as such.
  → Resolved 2026-09-23: added a paragraph to CLAUDE.md's "Chart creation" section positioning Segment (`mb segment create`) and Measure (`mb measure create`) as the right escalation for a reusable single-table filter/aggregation — narrower than a Model (MBQL-only, no joins), so a Model is still the answer once native SQL or a join is needed; also covered `mb card create`'s `metric` type and when it's worth weighing against a plain Model-backed chart (instance-wide reuse via the `library` skill, not the common single-dashboard case), the `revision_message` requirement on Segment/Measure updates, and writing a new one's confirmed mapping straight to `references/metric-glossary.md`. Separately added a paragraph to CLAUDE.md's "Before every session" section stating `data-workflow` is a deliberate non-fit — this project's own `prompts/discovery.md`/`requirements-intake.md`/`chart-generation.md` already encode that same end-to-end loop, specialized for Recruit CRM/Metabase's account-scoped collections, hard constraint 7, and business-term rules that the generic skill knows nothing about.

- 2026-09-23 — general — architecture — Project improvement review (standalone dashboard-documentation flow vs. CLAUDE.md's "Dashboard documentation" framing)
  CLAUDE.md's "Dashboard documentation" section opens with "a dashboard the Requirements Intake flow creates or adds to gets a companion Document," and "History log" only documents a Document being logged via `documentation_doc_id`/`documentation_doc_name` fields nested inside that same pass's `dashboard_created`/`dashboard_updated` entry — but `logs/history.jsonl`'s most recent entry (2026-09-23, account 101731) is a `documentation_created` entry for dashboard 18713, a pre-existing dashboard that this session did not create or add cards to at all (matches the pattern behind all three 2026-09-23 Resolved entries below for the same account/dashboard). `documentation_created` isn't one of CLAUDE.md's documented history-log types — it's only valid under the file's general "any other genuinely useful event... with a descriptive type" allowance — and Step 0.5's three named entry points (Requirements Intake / Default Dashboard / Important Metrics Dashboard) have no explicit "just add documentation to an existing dashboard" path either. Worth either naming this as its own explicit case in "Dashboard documentation" (and formalizing `documentation_created` as a real History-log type) or folding it into Requirements Intake's own steps as an explicit branch, since it's now a proven, recurring real pattern rather than a one-off.
  → Resolved 2026-09-23: added an explicit "documentation-only request" case to CLAUDE.md's "Dashboard documentation" section and `prompts/requirements-intake.md`'s "Add the documentation Document" section — it still enters through Requirements Intake (Step 0.5), but skips straight to confirming the account and the named existing dashboard, skips the Data Team WIP-vs-account-collection question entirely since no new content is created, and logs a standalone `documentation_created` entry instead of folding into a `dashboard_created`/`dashboard_updated` one. Formalized `documentation_created` as its own documented History-log type in CLAUDE.md (using the real account-101731 entry as the worked example) and corrected the prior "fold into the same entry... it isn't logged as its own separate entry type" line, which was flatly wrong for this now-proven, recurring case — it now states both cases (same-pass vs. documentation-only) explicitly.

- 2026-09-23 — account 101731 — core-function-quality — Executive Summary Guide, drill-down description stopped at the first hop
  Even after adding per-section "Clicking through:" lines grounded in real `click_behavior` (see the entry directly below), the description still stopped at the first destination — "opens the X detail table, pre-filtered by ..." — without checking whether that destination itself had a further layer. It did: every detail table on this dashboard has one or more "Profile" columns (`view_as: "link"`) whose raw cell value is a live `app.recruitcrm.io/...` URL, confirmed by sampling one real row — a second click that leaves Metabase entirely, not another internal view. The two FTSO sections go further still: their first click lands on a dedicated drill-down *dashboard* (with its own independent Date/User/Company/Team/Candidate filters) before that same Profile-link layer, making them a genuine three-step chain versus two steps everywhere else. The exact set of Profile columns also isn't uniform — Calls has only one (no job/company link at all), Client Meetings & Visits has five (Candidate/Contact/Deal/Job/Company, since one activity can be associated with any of those types) — a description copy-pasting "Candidate/Job/Company" everywhere would have been wrong on both of those tables.
  → Resolved 2026-09-23: added a "Don't stop at the first hop — check whether the destination itself has a further layer" subsection to `prompts/documentation.md` (open the destination's own `visualization_settings`, distinguish `click_behavior` from a `view_as: "link"` URL column, confirm a link column's real destination with one live sampled row rather than assuming, state the true number of steps per section since it can differ within one dashboard). CLAUDE.md's "Dashboard documentation" → "How to use it" bullet and `prompts/requirements-intake.md`'s equivalent branch both now require checking for this before treating a drill-down description as finished. The already-published Executive Summary Guide (document 298) was corrected in the same session this was found, before this backlog entry was written.

- 2026-09-23 — account 101731 — core-function-quality — Executive Summary Guide, "How to use it" never described drill-downs at all
  Even after the same-day filter-grounding fix (see the entry directly below), the "How to use it" section's drill-down coverage stayed a single generic sentence ("every chart is clickable... opens the individual records") — never naming an actual destination or what carries through, despite all 25 dashcards on this pre-existing dashboard already having `click_behavior` wired. Pulling the real data showed real, chart-specific structure worth documenting: two of the nine sections (the candidate-owner and job-owner FTSO groups) drill into a dedicated drill-down dashboard with its own independent filters rather than a plain detail table; and within the FTSO → Placement % group specifically, the three sibling charts forward different filter subsets to the identical destination (the scalar and by-owner bar pass Date/User/Company, the by-owner bar adds Team, the Over Time chart passes only Date) — the same kind of per-chart asymmetry already flagged for dashboard filters, just on the drill-down side, and nothing in this project's own instructions said to check for it.
  → Resolved 2026-09-23: added a "Describe each chart's drill-down from its real `click_behavior`" section to `prompts/documentation.md` (resolve `linkType`/`targetId` to the destination's real name, read `parameterMapping` per dashcard rather than assuming uniformity within a chart group, call out a pivot's dedicated drill-down dashboard as a distinct case with its own filters). CLAUDE.md's "Dashboard documentation" → "How to use it" bullet and `prompts/requirements-intake.md`'s equivalent step both now require this directly, with the latter also handling the two real orderings a session can hit: no drill-downs to describe yet (the flow's own normal cards-then-drill-downs sequencing) vs. a dashboard that already has them wired (this account's actual case, and generally true of any older dashboard a Document is being retrofitted for). The already-published Executive Summary Guide (document 298) was corrected in the same session this was found, before this backlog entry was written.

- 2026-09-23 — account 101731 — core-function-quality — Executive Summary Guide, first draft was ungrounded and its filter list was incomplete
  Building the companion Document for the (pre-existing) Executive Summary dashboard, the first draft's "what each chart means" paragraphs were written from each card's name/display type alone, not its actual `dataset_query` — resulting in speculative, unverifiable narrative in a document meant to be a source of truth (e.g. "a drop in outbound calls often shows up in FTSO a few weeks later", a causal claim no query supports). The "How to use it" section listed only 3 filters (Date, Time grouping, User) when the dashboard actually has 5 (also Company and Team), and implied every filter applies to every chart when several don't (the Client Meetings & Visits and Jobs Added sections have no Team filter at all; three specific trend cards don't respond to Time grouping despite having a date breakout; one card — FTSO → Placement % Over Time — only responds to Date and ignores User/Company/Team entirely). Separately, creating the document surfaced an undocumented Metabase behavior: embedding an existing card via a plain positive `cardEmbed` id still clones it into a new document-owned card row — confirmed as intended product behavior (`metabase/metabase#80775`), not a mistake, but nothing in this project flagged that a *second* `document update` referencing the original ids (instead of the live clone ids) would orphan the first batch and mint a fresh set.
  → Resolved 2026-09-23: added `prompts/documentation.md` (full method: ground every chart paragraph in the card's real `dataset_query`, walking into any `source-card`; determine per-chart filter applicability from the dashboard's real `parameters` + each dashcard's `parameter_mappings` rather than a blanket list; a "Known Metabase gotchas" log documenting the card-cloning behavior and the re-fetch-before-second-update rule). CLAUDE.md's "Dashboard documentation" section and `prompts/requirements-intake.md`'s "Add the documentation Document" step both now point to it and state the grounding requirement directly, so a future session builds this correctly from the start instead of needing the same correction. The already-published Executive Summary Guide (document 298) was corrected in the same session this was found, before this backlog entry was written.

- 2026-09-18 — general — consistency — Project improvement review (grounded in references/visual-design-standards.md vs. CLAUDE.md and the two dashboard scripts)
  `references/visual-design-standards.md` defines a mandatory categorical color palette ("never leave color to Metabase's own automatic assignment... a single-series chart still gets slot 1 set explicitly") and is already wired into `prompts/chart-generation.md`'s chart-creation step, but CLAUDE.md itself never mentions the file at all — unlike "Data Labels"/"Table Formatting"/"Combo Chart Series Display", which CLAUDE.md documents directly and explicitly calls out as applying to `scripts/create_default_dashboard.py` and `scripts/create_important_metrics_dashboard.py`. Checked both scripts' templates directly: zero `series_settings` entries anywhere across the ~30 multi-series-capable cards (pie/bar/line/row displays) in `scripts/default_dashboard_template.json` and `scripts/important_metrics_dashboard_template.json`, so every Default Dashboard and Important Metrics Dashboard card is left on Metabase's automatic color assignment — directly contradicting the standard's own scope statement that it governs "new charts and dashboards this project builds," with no carve-out anywhere for these two flows. Worth either adding a CLAUDE.md pointer to the standard (matching the Data Labels/Table Formatting treatment) and wiring explicit series colors into both templates, or explicitly scoping the standard to Requirements Intake only if that's actually intended.
  → Resolved 2026-09-18: the user asked to make `references/visual-design-standards.md` itself production-grade first (research-grounded, universal/non-client-specific) before touching the scripts. Rebuilt the file end-to-end — dashboard information hierarchy (inverted pyramid, 5-second rule, F-pattern, grouped by subject), form selection (dual-axis ban, series-count ladder, all-pairs cap, pie/donut caps), a new Ordinal color job for ordered categories like pipeline stages, cross-dashboard entity-color consistency, density-aware data labels, and KPI trend-direction color semantics — grounded in the `dataviz` skill's computable method plus live research against Metabase's own official dashboard-design guidance and Stephen Few's *Information Dashboard Design*. Added CLAUDE.md pointers ("Color", revised "Data labels", new "KPI trend color direction", a dual-axis note under "Combo chart series display") so the standard is discoverable from CLAUDE.md itself, not just `prompts/chart-generation.md`. Retrofitting the two automated scripts against the rebuilt standard is intentionally out of scope for this entry — logged as its own new Open item above.


- 2026-09-18 — general — metabase-currency — Project improvement review (mandatory `mb skills list` diff against CLAUDE.md's skill paragraph)
  CLAUDE.md's "Before every session" paragraph names every specialized `mb` skill this project touches beyond `mbql`/`dashboard`/`visualization` — `document`, `metadata`, `native-sql` — but omitted `notification`, even though `prompts/requirements-intake.md` (lines 80-85) already has live routing logic sending delivery-schedule requests ("email me this dashboard every Monday", "alert me when X drops below Y") to that exact skill via `mb subscription`/`mb alert`. A session skimming CLAUDE.md's skill list for what this project actually uses would have missed that `notification` is one of them.
  → Resolved 2026-09-18: added `notification` to CLAUDE.md's skill-list paragraph, with the same one-line "what it's for" treatment as `document`/`metadata`/`native-sql`, pointing at `prompts/requirements-intake.md`'s existing routing rule.

- 2026-09-17 — account 92840 — core-function-quality — Owner Activity dashboard, week/month/year breakdown gap
  Neither `prompts/requirements-intake.md` nor `prompts/chart-generation.md` said a "broken down by week/month/year" (or general "trend over time") requirement needs an actual date breakout in the query grain, not a flat "metric by category" aggregate. Without that rule, resolution had nothing telling it to reach for a time dimension, so the six Owner Activity cards got built as flat "metric by owner" bars with no time dimension at all, despite the client asking for it on the 2026-09-10 call.
  → Resolved 2026-09-18: added a new resolution rule (`prompts/requirements-intake.md`, "Resolving each requirement" item 6) requiring a date breakout at the requirement's implied grain, plus considering a dashboard `temporal-unit` parameter when more than one card would benefit from an adjustable grain. Note: the original entry cited card 75181 / dashboard 18713 as an "already live" example of both the query breakout *and* a wired `temporal-unit` dashboard parameter — verified during this fix (`mb card get 75181 --full --json`, `mb dashboard get 18713 --json`) that only the fixed-grain query breakout (`temporal-unit: "month"` hardcoded in the card's own MBQL, no dashboard-level parameter) actually exists; dashboard 18713 currently has no `parameters` at all. The new rule is written from the general, `dashboard`-skill-confirmed mechanism rather than relying on that specific example, since the dashboard-parameter half of it didn't hold up under verification.

- 2026-09-17 — general — consistency — Project improvement review (mandatory memory-layer consistency check, per `prompts/project-improvement.md`)
  The 2026-09-15 fix (write-back instructions tightened to "same turn") explicitly flagged itself as unverified pending a future account's actual glossary section. Checked 2026-09-17 and re-checked 2026-09-18: `logs/history.jsonl` shows real, substantial Requirements Intake work (recommendations, charts, dashboards, drill-downs) for accounts 38992, 44663, 62956, 96467, and 101731 — none of them has a `## Account <n>` section in `references/metric-glossary.md`, unchanged between the two checks.
  → Resolved 2026-09-18, in two parts: (1) the structural fix — a required "Final recap" step in `prompts/requirements-intake.md` that re-reads `metric-glossary.md` before logging/wrapping up to confirm every confirmed term actually landed — was already added prior to this review and is now confirmed still in place, so this part needed no further action. (2) The retroactive-reconstruction question is decided, not left open: declining to reconstruct these 5 accounts' glossary sections after the fact. Inferring what currency/hiring-stage-order/term mapping was actually confirmed by mining the already-created cards' `dataset_query` would be exactly the "side door back to discovering real values without asking" CLAUDE.md's "Avoiding duplicate charts" section forbids for a different case — there's no reliable way to tell a genuinely user-confirmed value apart from a guessed one after the fact, so reconstructing risks recording a fabricated "confirmed" answer as if a human had actually stated it. These 5 accounts' glossary sections stay unpopulated as a known, accepted gap; any future session touching one of them should treat it as a fresh "ask once" account rather than assuming an undocumented prior answer.

- 2026-09-18 — account 89060 — core-function-quality — Cleveland Clinic dashboard, table-formatting polish applied by hand twice
  During manual review, the user applied the identical visualization-settings pattern to two different `table`-display cards (Activity Overview, Weekly Outreach) independently: center-aligned columns, the raw `date` field renamed to "Date" with abbreviation, a subtle row highlight (`#F9F9FA`, `highlight_row: true` on a not-null condition), and `table.cell_column` set to the first metric column for in-cell mini-bar rendering. Initially logged both as `built_modified` with `root_cause: {fixable: false, fix_type: "none_cosmetic"}` per `prompts/performance-tracking.md`'s new root-cause-check step — the user correctly pushed back: two independent applications of one identical pattern is a real, repeatable design standard this project should apply by default, not a one-off preference with nothing to fix.
  → Resolved 2026-09-18: added a "Table formatting" section to CLAUDE.md (new subsection alongside "Data labels"/"Value formatting") encoding the exact pattern (center-align, date column cleanup, row highlight, `table.cell_column`) as a default for every qualifying `table`-display card this project builds going forward, including the two automated dashboard scripts.

- 2026-09-18 — account 89060 — workflow-friction — Cleveland Clinic dashboard, Final Interviews to Accepts stage substitution
  This card was originally built correctly on a documented, confirmed substitute stage (`references/metric-glossary.md`'s "Final Interview Completed" entry — the client's literal term has zero matching rows; the substitute was confirmed in-session and tagged `caveat` for thin data). During manual review the user swapped it to the client's literal term across two live edits before landing on "accept it returns 0" — a decision they could have made immediately if the caveat had been surfaced as part of the card's own report-back, not left to be found by opening `metric-glossary.md` or by the card quietly showing 0 live. Logged per `prompts/performance-tracking.md`'s new root-cause-check step (a `built_modified` card whose root cause is a project-level reporting gap, not an account-specific fact — the glossary entry itself was already correct).
  → Resolved 2026-09-18: added a rule to `prompts/chart-generation.md` step 8 — when a card's confirmed mapping traces back to a `Status: caveat` line in `references/metric-glossary.md`, say so explicitly in the same message that reports the card created, rather than leaving it implicit in the glossary for the user to discover only once the live number looks wrong.

- 2026-09-15 — general — consistency — Project improvement review (grounded in CLAUDE.md vs. scripts/)
  CLAUDE.md's "Drill-downs" section states its requirement ("every card ... should carry an explicit click_behavior wherever a sensible drill target exists") with no scoping to Requirements Intake only, unlike the parallel "Dashboard documentation" section which explicitly exempts the Default Dashboard and Important Metrics Dashboard flows. But neither `scripts/create_default_dashboard.py` nor `scripts/create_important_metrics_dashboard.py` (nor their templates) wires any `click_behavior` — they only create the empty "Drill-downs" sub-collection structurally. Worth either adding an explicit scope note to "Drill-downs" (matching the documentation-tab carve-out) or implementing drill-downs in the two scripts — whichever is actually intended.
  → Resolved 2026-09-15: implemented drill-downs in both scripts (new shared `scripts/dashboard_drilldowns.py`, `drilldown_entities`/`drilldown_cards`/per-card `drill` keys added to both templates) rather than exempting them — every qualifying dashcard across both dashboards now gets a `click_behavior` (single dashcard-level into a shared per-entity detail list for the large majority; per-column for the one genuinely multi-metric table card; a dedicated drill-down dashboard for the one multi-value pivot), with the "already most granular" carve-out applied to the one unaggregated table card. Added a clarifying line to CLAUDE.md's "Drill-downs" section pointing at this.

- 2026-09-15 — general — consistency — Project improvement review (grounded in this file's own structure)
  This file's very last line is a dangling `-->` with no matching `<!--` before it (after the 2026-08-19 Resolved entry), left over from an earlier template edit — the file that's supposed to catch drift in the rest of the project carries an unfixed comment-structure glitch of its own. Worth deleting that stray closer.
  → Resolved 2026-09-15: removed the stray trailing `-->`.

- 2026-09-15 — general — consistency — Project improvement review (grounded in prompts/metabase_skill_improvement.md)
  `prompts/metabase_skill_improvement.md` — still the file `prompts/requirements-intake.md` points to for how to build `references/canonical-patterns.md` once that's undertaken — tells its reader to use "the `mb` CLI / Metabase MCP connector" (twice: lines 12 and 64) to inspect tables and search existing work. That directly contradicts CLAUDE.md hard constraint 1, "Metabase CLI (`mb`) is the only interface to data and analytics." No other file in the project mentions MCP at all, so this reads as drift from an earlier draft rather than an intended exception. Worth stripping the MCP mentions from that file so a future session building canonical-patterns.md isn't handed a green light to use a forbidden interface.
  → Resolved 2026-09-15: stripped both MCP mentions from `prompts/metabase_skill_improvement.md` (lines 12 and 64) — it now points only to the `mb` CLI, consistent with hard constraint 1.

- 2026-09-15 — general — robustness — Project improvement review (grounded in account 44663's Sourcing Report drill-down history)
  Mid-session on 44663, `mb card query --parameters` returned a negative result for a plain-MBQL custom-destination drill-down, which was read as "this whole mechanism doesn't work" and triggered a full rebuild (native SQL, then an alternate on-dashboard Detail-tab-with-parameter_mappings architecture) before a real UI-built example proved the original plain-MBQL `click_behavior` approach was fine all along. The actual cause — `mb card query --parameters` doesn't exercise the same code path as real click-through navigation — is stated once in `logs/history.jsonl` but isn't in `prompts/drilldowns.md`'s "Known Metabase gotchas" list. Worth adding as its own bullet there: a negative result from that command is not proof a click_behavior mechanism is broken, so don't let it justify an architecture rebuild — check against a confirmed UI-built example first, per the gotcha bullet already there for guessing the shape itself wrong.
  → Resolved 2026-09-15: added this as its own "Known Metabase gotchas" bullet in `prompts/drilldowns.md`, right after the "guessed wrong twice" bullet it builds on.

- 2026-09-15 — general — robustness — Project improvement review (grounded in account 44663's recent drill-down history)
  Three separate follow-up sessions each fixed a different completeness gap in the same dashboard's drill-downs after the fact (a missing breakout dimension in click_behavior, a filter-bound field left invisible, no independently-actionable context field) — all against rules already documented at the time.
  → Resolved 2026-09-15: added a "Completion audit" section to `prompts/drilldowns.md` (a 7-point checklist run once after wiring a batch, before reporting it done) and a corresponding required bullet in CLAUDE.md's condensed "Drill-downs" section.

- 2026-09-15 — general — consistency — Project improvement review (grounded in references/metric-glossary.md)
  metric-glossary.md's "Term mappings (this account only)" section is one flat, unlabeled list, but it already holds mappings from at least two different accounts (a 662-context set plus an explicit `assign_job_candidate_53181` entry) with nothing marking which account each mapping belongs to — a future session on a different account could mistake one account's stage/field mapping as its own. Worth restructuring the file into per-account sections (e.g. a `## Account <n>` heading per account) so CLAUDE.md's "never carry a mapping forward from one account to another" rule is actually enforceable by the file's own shape, not just by convention.
  → Resolved 2026-09-15: restructured `references/metric-glossary.md` into an account-agnostic "Open questions (template)" section plus `## Account <n>` sections for confirmed per-account answers. Only the `assign_job_candidate_53181` mapping had a recoverable account number, so it stayed flagged for a future move into its own `## Account 53181` section; every other prior mapping (no recoverable account) moved to a clearly-labeled "Unattributed" section that must not be reused for any specific account without reconfirming with the user.

- 2026-09-15 — general — consistency — Project improvement review (grounded in references/metric-glossary.md and logs/history.jsonl)
  `logs/history.jsonl` shows real chart/dashboard work across at least 10 distinct accounts (662, 2227, 6268, 34301, 43720, 51254, 62956, 80402, 44663, 38992, plus 92840 and 96467 appearing elsewhere), yet `references/metric-glossary.md` has zero populated `## Account <n>` sections — only the illustrative "Account 662" template example and legacy pre-restructure "Unattributed" entries. Since CLAUDE.md/discovery.md/requirements-intake.md all instruct recording a confirmed answer (currency, hiring-stage order, term mapping) back to this file the first time it's resolved for an account, this gap suggests those confirmed answers are being resolved in-session but never written back — so the "ask once per account" design is silently failing and each new session likely re-asks a question a prior session already got answered.
  → Resolved 2026-09-15: tightened the write-back instruction everywhere a business-term/definitional answer gets confirmed — CLAUDE.md ("Data discovery", "Value formatting", and the hiring-stage-order paragraph), `prompts/discovery.md` section 4, `prompts/chart-generation.md`'s currency step, and `prompts/requirements-intake.md`'s term-mismatch step — from "record the answer" (a deferrable-sounding instruction) to "write it immediately, in the same turn it's confirmed, before moving on." Whether this actually closes the gap (vs. sessions still not doing it) needs checking against a future account's actual glossary section once one goes through the flow again.
  (An initial version of this fix also told live discovery to auto-append new structural facts to `references/schema-map.md` the moment it found them. Corrected 2026-09-15, same day: `schema-map.md` is deliberately stable and manually curated — its per-account write-once treatment doesn't apply there. Discovery now flags a structural discrepancy to the user instead of writing it in; a confirmed schema change reaches the file only as its own separate, reviewed edit.)

- 2026-08-19 — account 662 — workflow-friction — Requirements Intake, recruiter performance dashboard
  The currency question gets asked even when every chart in the request is
  a plain count — worth skipping it until a monetary field is actually
  about to be formatted.
  → Resolved 2026-08-22: CLAUDE.md "Value formatting" now only asks when
    the confirmed chart set includes at least one monetary field.

- 2026-09-24 — account 662 — core-function-quality — Verifying the new dynamic pie-color logic against account 662's real data (scripts/important_metrics_dashboard_template.json, "Candidate Distribution By City")
  Running this card's own query live (required to build its `pie.colors`) surfaced that its breakout field, `candidates_662.city`, has 4,177 distinct values on this account — not clean city names: the top values by volume are blank/empty (59,863 rows), followed by free-text garbage (a stray "Female," multi-line notes, literal dates). A pie chart on a field this dirty and this high-cardinality directly violates `references/visual-design-standards.md`'s own "Pie/donut stays small" cap (≤ ~6 categories) and CLAUDE.md's "Data quality gate" ("suspicious distributions... drop it and note internally why"), independent of coloring — Metabase's `pie.slice_threshold` will fold the long tail into "Other" visually, but the chart's core premise (a small number of meaningful city groupings) doesn't hold against this field's actual content. Unclear whether this is specific to account 662 (Recruit CRM's own internal test/QA account, per CLAUDE.md, so genuinely likely to carry throwaway test data other accounts wouldn't) or a real risk on any account with an unstructured `city` field.
  → Resolved 2026-09-24 (user said "you decide" on the remaining chart-design question): first added `CATEGORICAL_COLOR_CARDINALITY_CAP = 100` to `apply_series_colors`/`assign_categorical_colors` in both scripts (above 100 distinct values, nothing gets colored at all rather than an arbitrary top-8 guess) — that alone made the *coloring* safe but left the underlying chart-design problem (a "city" free-text field is structurally unbounded-cardinality on any account with real geographic spread, not just an account-662 data-quality fluke) untouched. Decided the card itself needed to change, not just be left uncolored: in `scripts/important_metrics_dashboard_template.json`, "Candidate Distribution By City" is now a `row` chart (not `pie`) of the top 10 cities by candidate count, with a `not-empty` filter excluding blank/null city values (an absence of data isn't a "distribution" category) and standard single-series slot-1-blue coloring — the same shape as the template's other single-series "Distribution of X" row cards (e.g. "Distribution of Candidates by Source"), which sidesteps the cardinality problem entirely rather than working around it. Deliberately did **not** try to clean/normalize the real city values themselves (e.g. the still-present "Female" and "Gopalganj" outliers on account 662) — that's real account data, not this project's to alter, per CLAUDE.md's "never fabricate data." Verified end-to-end against account 662 live: dry-run passes, the live query correctly excludes the 59,863 blank rows and returns the top 10 real values, and the redesigned card (being single-series) is correctly left alone by `apply_series_colors`. Checked `logs/history.jsonl` first — only account 662's own test-collection runs have ever used this template, so no real client dashboard needs a retroactive fix.
